
import { formatDyn } from 'ethereum-pool/helpers/format-dyn';
import { module, test } from 'qunit';

module('Unit | Helper | format dyn');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatDyn([42]);
  assert.ok(result);
});

