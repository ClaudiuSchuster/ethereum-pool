<div class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container">
  
<!-- MOBILE NAVIGATION -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle dropdown" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
      </button>
      
      <ul class="dropdown-menu nav navbar-nav navbar-mobile">
        {{#active-li currentWhen='index'}}
          {{#link-to 'index'}}
            <i class="fa fa-home"></i> Home
          {{/link-to}}
        {{/active-li}}
        {{#active-li currentWhen='blocks'}}
          {{#link-to 'blocks'}}
            <i class="fa fa-cubes"></i> Pool Blocks
            {{#if model.maturedTotal}}
              <span class="badge alert-success">{{model.maturedTotal}}</span>
            {{/if}}
          {{/link-to}}
        {{/active-li}}
        {{#active-li currentWhen='payments'}}
          {{#link-to 'payments'}}
            <i class="fa fa-paper-plane-o"></i> Payments
            {{#if model.paymentsTotal}}
              <span class="badge alert-success">{{model.paymentsTotal}}</span>
            {{/if}}
          {{/link-to}}
        {{/active-li}}
        {{#active-li currentWhen='miners'}}
          {{#link-to 'miners'}}
            <i class="fa fa-users"></i> Miners
            {{#if model.minersTotal}}
              <span class="badge alert-success">{{model.minersTotal}} @ {{format-hashrate model.hashrate}}</span>
            {{/if}}
          {{/link-to}}
        {{/active-li}}
		{{#active-li currentWhen='finders'}}
          {{#link-to 'finders'}}
            <i class="fa fa-diamond"></i> Finders
          {{/link-to}}
        {{/active-li}}
      </ul>
      
      <a class="navbar-brand"><span class="logo-1">ice</span><span class="logo-2">mining</span></a>
    </div>
    
<!-- DESKTOP NAVIGATION -->
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        {{#active-li currentWhen='index'}}
          {{#link-to 'index'}}
            <i class="fa fa-home"></i> Home
          {{/link-to}}
        {{/active-li}}
        {{#active-li currentWhen='blocks'}}
          {{#link-to 'blocks'}}
            <i class="fa fa-cubes"></i> Pool Blocks
            {{#if model.maturedTotal}}
              <span class="badge alert-success">{{model.maturedTotal}}</span>
            {{/if}}
          {{/link-to}}
        {{/active-li}}
        {{#active-li currentWhen='payments'}}
          {{#link-to 'payments'}}
            <i class="fa fa-paper-plane-o"></i> Payments
            {{#if model.paymentsTotal}}
              <span class="badge alert-success">{{model.paymentsTotal}}</span>
            {{/if}}
          {{/link-to}}
        {{/active-li}}
        {{#active-li currentWhen='miners'}}
          {{#link-to 'miners'}}
            <i class="fa fa-users"></i> Miners
            {{#if model.minersTotal}}
              <span class="badge alert-success">{{model.minersTotal}} @ {{format-hashrate model.hashrate}}</span>
            {{/if}}
          {{/link-to}}
        {{/active-li}}
      </ul>
    </div>
    
    
  </div>
</div>

{{outlet}}


