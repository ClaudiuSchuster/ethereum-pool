
import { formatAddr } from 'ethereum-pool/helpers/format-addr';
import { module, test } from 'qunit';

module('Unit | Helper | format addr');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatAddr([42]);
  assert.ok(result);
});

